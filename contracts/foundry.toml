[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.29"
via_ir = true
optimizer = true
optimizer_runs = 200

[profile.default.fmt]
line_length = 120

[rpc_endpoints]
arbitrum = "${ARBITRUM_RPC_URL}"
